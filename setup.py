#!/usr/bin/env python

from distutils.core import setup
import sys

setup(name = 'execo',
      version = '1.0',
      packages = None,
      py_modules = [ 'execo', 'execo_g5k' ],
      author_email = 'matthieu.imbert@inria.fr',
      )
